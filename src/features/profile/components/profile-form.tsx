"use client";
import { useState } from "react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
type Values = { bio: string; course: string; department: string; studentId: string; yearOfStudy: number | null };
export function ProfileForm({ initialValues }: Readonly<{ initialValues: Values }>) { const [values, setValues] = useState(initialValues); const [saving, setSaving] = useState(false); async function save() { setSaving(true); const response = await fetch("/api/profile", { body: JSON.stringify(values), headers: { "Content-Type": "application/json" }, method: "PATCH" }); setSaving(false); if (!response.ok) { toast.error("Unable to save profile."); return; } toast.success("Profile updated."); } return <div className="grid gap-3 sm:grid-cols-2"><Input placeholder="Student ID" value={values.studentId} onChange={(event) => setValues({ ...values, studentId: event.target.value })} /><Input placeholder="Department" value={values.department} onChange={(event) => setValues({ ...values, department: event.target.value })} /><Input placeholder="Course" value={values.course} onChange={(event) => setValues({ ...values, course: event.target.value })} /><Input min="1" placeholder="Year" type="number" value={values.yearOfStudy ?? ""} onChange={(event) => setValues({ ...values, yearOfStudy: event.target.value ? Number(event.target.value) : null })} /><textarea className="min-h-28 rounded-xl border border-border bg-background-elevated p-3 text-sm sm:col-span-2" placeholder="Tell the community about yourself" value={values.bio} onChange={(event) => setValues({ ...values, bio: event.target.value })} /><Button disabled={saving} className="sm:col-span-2" onClick={save}>{saving ? "Saving..." : "Save profile"}</Button></div>; }
