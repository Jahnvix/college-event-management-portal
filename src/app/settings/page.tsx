import { BellRing, CalendarClock, Mail } from "lucide-react";
import { DashboardShell } from "@/components/layouts/dashboard-shell";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { PreferenceToggle } from "@/features/profile/components/preference-toggle";
import { getCurrentUser } from "@/server/auth";
import { prisma } from "@/server/database";
export default async function SettingsPage() { const user = await getCurrentUser(); if (!user?.id) return null; const profile = await prisma.profile.findUnique({ where: { userId: user.id } }); const preferences = [{ icon: Mail, label: "Email announcements", enabled: profile?.emailAnnouncementsEnabled ?? true, field: "emailAnnouncementsEnabled" as const }, { icon: BellRing, label: "In-app notifications", enabled: profile?.inAppNotificationsEnabled ?? true, field: "inAppNotificationsEnabled" as const }, { icon: CalendarClock, label: "Calendar sync", enabled: profile?.calendarSyncEnabled ?? false, field: "calendarSyncEnabled" as const }]; return <DashboardShell><div className="mx-auto max-w-3xl space-y-7"><header><p className="text-xs font-bold tracking-[0.2em] text-accent uppercase">Preferences</p><h1 className="mt-2 font-display text-3xl font-bold">Stay informed your way.</h1></header><Card><CardHeader><CardTitle>Notification channels</CardTitle><CardDescription>Changes are saved immediately to your student profile.</CardDescription></CardHeader><CardContent className="divide-y divide-border">{preferences.map(({ icon: Icon, label, enabled, field }) => <div className="flex items-center justify-between gap-5 py-4" key={field}><div className="flex items-center gap-3"><div className="rounded-xl bg-secondary p-2 text-primary"><Icon className="size-4" /></div><span className="text-sm font-bold">{label}</span></div><PreferenceToggle enabled={enabled} field={field} label={label} /></div>)}</CardContent></Card></div></DashboardShell>; }
